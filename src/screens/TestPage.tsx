import {AppScreen} from '@components';
import {Text} from '@theme';
import React, {useLayoutEffect} from 'react';
import {View} from 'react-native';

export const TestPage = ({navigation}: any) => {
  return (
    <AppScreen>
      <Text>test page</Text>
    </AppScreen>
  );
};
