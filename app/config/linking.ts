const config = {
    screens: {
        AppStack: '',
    },
};
const linking = {
    prefixes: ['reactNativeTypescript://app', 'https://reactnativetypescript.com'],
    config,
};

export default linking;
